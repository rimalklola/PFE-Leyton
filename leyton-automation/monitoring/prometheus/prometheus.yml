global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/rules.yml

scrape_configs:
  - job_name: leyton-api
    static_configs:
      - targets: ['api:8000']
    metrics_path: /metrics
